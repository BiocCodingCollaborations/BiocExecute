% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scriptsParser.R
\name{scriptParser}
\alias{scriptParser}
\title{Parse a BiocExecute script}
\usage{
scriptParser(scriptPath)
}
\arguments{
\item{scriptPath}{A path to the R script to parse.}
}
\value{
A list with two character-vector elements:
\describe{
\item{header}{The optional leading \verb{#|} annotation lines.}
\item{content}{The remaining script lines, preserved unchanged.}
}
}
\description{
Read an R script and separate its optional Rapp annotation header from its
executable content.
}
\details{
The header consists of consecutive \verb{#|} annotation lines at the beginning
of the file. It may contain a \code{name} field, but this field is optional. When
present, the name must be defined only once and must be a valid R object
name. A script must not start with a \verb{#!} header because the compiled
executable provides its own Rapp shebang.
}
\examples{
script <- tempfile(fileext = ".R")
writeLines(c("#| name: example", "", "value <- 1"), script)
scriptParser(script)
unlink(script)

}
